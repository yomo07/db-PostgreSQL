{
  "$schema": "https://agent-plugins.org/schemas/1.0.0/mcp.schema.json",
  "mcpServers": {
    "postgres": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@microsoft/postgres-mcp", "run"],
      "env": {
        "PGSQL_MCP_CONNECTION_STRING": "${PGPOWER_DSN}",
        "PGSQL_MCP_PROFILE_NAME": "dbpostgres"
      }
    }
  }
}
