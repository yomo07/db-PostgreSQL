{
  "$schema": "https://agent-plugins.org/schemas/1.0.0/mcp.schema.json",
  "mcpServers": {
    "postgres": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@microsoft/postgres-mcp", "run"],
      "env": {
        "POSTGRES_MCP_CONNECTION_STRING": "postgresql://testuser:testpass123@127.0.0.1:5442/inventario",
        "POSTGRES_MCP_PROFILE_NAME": "dbpostgres"
      }
    }
  }
}
